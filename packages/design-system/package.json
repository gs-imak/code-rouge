{
  "name": "@code-rouge/design-system",
  "version": "0.0.0",
  "private": true,
  "description": "Shared tokens, themed components, and platform variants (.tsx for RN, .web.tsx for Electron)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src --ext .ts,.tsx --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet' && exit 0"
  }
}
