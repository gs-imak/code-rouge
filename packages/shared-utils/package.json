{
  "name": "@code-rouge/shared-utils",
  "version": "0.0.0",
  "private": true,
  "description": "Persistence helpers, retry, date helpers, network probes — pure functions, deeply tested",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src --ext .ts --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet' && exit 0"
  }
}
