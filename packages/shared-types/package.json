{
  "name": "@code-rouge/shared-types",
  "version": "0.0.0",
  "private": true,
  "description": "Zod schemas, WebSocket message contracts, and JSON config types — single source of truth",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src --ext .ts --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet' && exit 0"
  }
}
